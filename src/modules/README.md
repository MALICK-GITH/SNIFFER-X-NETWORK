# Modules

SNIFFER-X-NETWORK v0.4 separates reusable network intelligence helpers from the interactive UI.

Planned modules: DNS, HTTP, TLS, links, PCAP, reports and platform-specific capture backends.

All remote checks are passive/public or simple TCP connections. Use only against systems you own or are authorized to test.
